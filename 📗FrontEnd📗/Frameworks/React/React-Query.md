## React-Query

`react-query`는 서버 사이드 상태 관리를 위한 도구입니다.

보통 상태 관리 도구들은 동기 방식으로 동작하기 때문에 비동기로 서버의 상태를 조회해서 넣어주는 로직들은 보통`redux-saga`같은 미들웨어로 구현합니다.

단순히 동작 하나 추가하려고 엄청난 코드를 생산하는 비효율성을 해결하고자 클라이언트-서버 사이드 상태 분리를 위한 `react-query`에 대해서 알아보겠습니다.

```bash
npm i react-query
```